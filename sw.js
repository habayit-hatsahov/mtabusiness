// §408 — v91 (נתיב-המילוט "שלחו לי קוד" נחתך שנייה לפני היעד). welcome.html + admin-dashboard.html.
// ⚠️ הבאמפ הוא התיקון עצמו: מכשיר שממשיך להריץ את ה-HTML מהמטמון ימשיך לוותר ב-8 שניות,
// כלומר בדיוק לפני שהכתובת השלישית עונה — ואצלו זו הכתובת היחידה שעובדת.
// §407 — v90 (המייל שגוגל אימתה נשמר על כשל-כניסה). welcome.html + admin-dashboard.html.
// ⚠️ **הכלל ב-firestore.rules נפרס ראשון** (`firebase deploy --only firestore:rules`, אומת
// מקצה-לקצה), כי `hasOnly` הוא רשימת-היתר: בסדר ההפוך **כל** אירוע loginFail היה נדחה
// בשקט — לא רק החדשים.
// §405+§406 — v89. welcome.html + admin-dashboard.html.
// ⚠️ **הבאמפ כאן הוא חלק מתיקון-אבטחה ולא ניקיון**: §406 סוגר מרוץ שבו זהות שכבר הייתה
// במכשיר יכלה לנצח את הכניסה החדשה. מכשיר שממשיך להגיש `welcome.html` מהמטמון ממשיך
// להריץ את הגרסה הפגיעה — ובדיוק במכשיר משפחתי/משותף, שהוא התרחיש היחיד שבו זה רלוונטי.
// §404 — v88 (זיהוי האוהד בשורות "לא מזוהה"). welcome.html + admin-dashboard.html.
// ⚠️ v87 **כבר נפרס**, ולכן זהו באמפ נפרד ולא הרחבה שלו: מכשירים שכבר קיבלו את v87
// מחזיקים אותו במטמון, ובלי מספר חדש welcome.html לא היה נרשם עם הזיהוי (§404) והשורה
// הייתה נשארת "לא מזוהה" בדיוק אצל מי שהתיקון נועד לו.
// §402+§403 — v87. שני סעיפים בבאמפ אחד, כי הם נפרסו יחד.
// §403 נגע ב**שמונה** קבצי HTML (הדד-ליין הופץ לכולם) — welcome, home, profile, business,
// business-dashboard, fan-register, terms, admin-dashboard. ⚠️ קובץ שיישאר במטמון ימשיך
// להריץ גיבוי שאינו מופעל על תלייה, כלומר בדיוק התקלה שתוקנה.
// §402 — דד-ליין לכל כתובת ב-hbApiFetch + "מה קרה בפועל" בתקלות הכניסה.
// ⚠️ **הבאמפ כאן הוא לב התיקון ולא ניקיון אחריו.** הקובץ שהשתנה הוא `welcome.html`, כלומר
// **דף הכניסה עצמו** — ומי שמריץ אותו מהמטמון ממשיך להריץ את הגרסה שבה כתובת תלויה לא
// מקדמת לכתובת הבאה, כלומר בדיוק הכשל שתוקן. וגרוע מזה: מדובר בדיוק באוהדים שכבר נכשלו
// פעם (הם הביקורים החוזרים), כלומר **מי שהכי צריך את התיקון הוא היחיד שלא יקבל אותו**
// (project_pwa_login_stale_sw). admin-dashboard.html מצטרף לאותו באמפ.
// §290 — v22. גרסת המטמון לא עלתה מאז §283, בזמן ש-§284–§290 שינו את fan-register.html
// שינוי אחר שינוי. הניווטים כאן הם network-first עם timeout של 1.5 שניות (ר' fetch למטה),
// ובמובייל סלולרי זה נופל למטמון בקלות — כלומר מכשיר יכול היה להמשיך להריץ HTML ישן
// לאורך כל סבבי התיקון, ולהיכשל מסיבה שכבר תוקנה. שינוי השם מפנה את המטמון הישן.
// §333 — v37. אותו תרחיש שהתיעוד למעלה מתאר, הפעם בפועל: אחרי §327–§332 (סדר האריחים,
// תמונות חדשות, מנגנון img-ready) ה-PWA של המשתמש המשיך להגיש HTML ישן — הרשת שלו איטית
// מ-1.5 שניות, ולכן הניווט נופל למטמון בכל פתיחה. הפרשי-התנהגות בין ספארי (חדש) ל-PWA
// (ישן) על אותו מכשיר הם החתימה של זה בדיוק. שינוי השם מוחק את כל המטמון הישן ב-activate.
// §337 — v39 (הפצת הגופן ל-6 דפים נוספים). §336 — v38. **חוק: כל שינוי ב-HTML מחייב העלאת המספר הזה.** בלי זה `reg.update()` שנוסף
// ב-§336 לא מוצא הבדל ב-sw.js, ה-activate לא רץ, המטמון הישן לא נמחק — וכל מנגנון כפיית
// העדכון הופך לחסר-תועלת. זה הצומת היחיד שבו "שכחתי להעלות גרסה" נכשל בשקט.
// §373 — v57 (נפילה-לאחור ל-API ב-home.html). הבאמפ הוא חלק מהתיקון:
// מי שממשיך להריץ את הגרסה הישנה ימשיך לנסות כתובת אחת בלבד.
// §371+§372 — v56 (חיבור חשבון Google בתפריט הצדדי). home.html + profile.html.
// ⚠️ הבאמפ נשכח בקומיט עצמו ונוסף אחריו. הניווטים הם network-first ולכן רובם
// היו מקבלים את החדש בכל מקרה — אבל בדיוק מי שהרשת שלו איטית נופל למטמון,
// והוא בדיוק מי שצריך כניסה קלה יותר. זה הצומת שבו "שכחתי גרסה" נכשל בשקט.
// §367 — v54 ("אושרו ולא נכנסו מעולם" בפאנל הפלטפורמה). admin-dashboard.html בלבד —
// אותו נימוק כמו §365: הקובץ נטען כדף רגיל ולכן כפוף לאותו מטמון.
// §360 — v49 (`user === null` הפסיק למחוק את הזיהוי מהמכשיר). שלושה קבצי HTML השתנו:
// welcome.html, home.html, profile.html — שלושתם נשאו את אותו ענף. ⚠️ הבאמפ כאן הוא חלק
// מהתיקון: אוהד שממשיך להריץ את ה-HTML הישן ימשיך לאבד את הזיהוי בכל גמגום של IndexedDB,
// כלומר בדיוק התקלה שתוקנה. ר' החוק בשורות למטה.
// §361 — נבלע לתוך v51 של §362 (סשן מקביל, אותו יום) ולא הופץ כ-v50 בנפרד. שני קבצי
// ה-HTML שלו נכללים ממילא בבאמפ ההוא. שניהם היו דורשים אותו מאותה סיבה שתועדה כאן:
//   business.html         — הוא הקובץ שמכיל את שער-הכפילות ואת שורת-ההמתנה. בעל-עסק
//                           שמקבל מהמטמון את הגרסה הישנה ימשיך לראות "שולח..." אילם
//                           ולהירשם פעמיים, כלומר בדיוק התקלה שדווחה.
//   admin-dashboard.html  — network-first עם 4 שניות (ר' §351+§352 למטה): מנהל על רשת
//                           איטית היה ממשיך לראות כרטיס-אוהד בלי תאי "לא מולא/לא נשאל".
// §362 — v51 (כתובת-פרופיל שנכתבה בסדר הפוך + handle אינסטגרמי עם קו-תחתון). ⚠️ הבאמפ
// כאן חובה **מסיבה שונה מכל הקודמות בקובץ הזה**, וחשוב שתיקרא: עיקר התיקון אינו ב-HTML
// אלא ב-`social-links.js`, וקבצים שאינם ניווט מוגשים כאן **stale-while-revalidate**
// (`return cached || network`, ר' ה-fetch למטה) — כלומר המטמון מנצח תמיד, והרשת רק
// מעדכנת לפעם הבאה. בלי החלפת שם-המטמון, אוהד או מנהל שכבר ביקר באתר היה מקבל את המנוע
// **הישן** בביקור הראשון אחרי הפריסה, ורואה בדיוק את הקישור המת שתוקן. שלושת קבצי ה-HTML
// (fan-register.html, business.html, admin-dashboard.html) מצטרפים לאותו באמפ.
// §355+§356 — v47 (הטלפון על כשל-כניסה, חתימת-זמן, "טיפלתי" שמפנה שורה, וקובייה אחת
// לכל התקלות). ⚠️ **הבאמפ כאן הוא חלק מהתיקון ולא ניקיון**, משתי סיבות נפרדות:
//   welcome.html          — הוא זה ששולח את שדה ה-phone. מכשיר שממשיך לקבל את הגרסה
//                           הישנה מהמטמון ימשיך לרשום loginFail **בלי טלפון**, כלומר
//                           התיקון ייראה כאילו אינו עובד בדיוק אצל מי שהכי צריך אותו.
//   admin-dashboard.html  — אינו רושם SW בעצמו אבל נשלט על ידיו (scope "/", ר' §352),
//                           והניווט הוא network-first עם 4 שניות: מנהל על רשת איטית היה
//                           מקבל את המרכז הישן, בלי הקובייה המאוחדת.
// בנוסף, תג-הסביבה של §338 מדווח 'sw-v47' — וזה מה שיבדיל במדידה בין אוהד שכבר מריץ את
// הקוד ששולח טלפון לבין אוהד שעדיין על v46, בלי לנחש.
// §377 — v60 (חיבור Google בתפריט שלשת הדפים + google-link.js חדש).
// ⚠️ קובץ JS חדש נכנס ל-stale-while-revalidate — ביקור ראשון מביא אותו מהרשת, אבל
// ה-HTML שמפנה אליו חייב להיות החדש — ולכן הבאמפ.
// §375+§376 — v59 (נפילה-לאחור ל-API בארבעת הקבצים שנשארו מאחור + דחיסת לוגו + באנר כשל-העלאה).
// ⚠️ **הבאמפ כאן הוא חלק מהתיקון, ובאותה מלכודת בדיוק של §374:** §375 נגע ב-admin-dashboard.html,
// business.html, business-dashboard.html ו-profile.html — כל ארבעתם נשלטים ע"י ה-SW (scope "/"),
// ו-admin-dashboard.html הוא network-first עם 4 שניות. כלומר המנהל שהרשת שלו לא פותרת את
// api.yellowzone.co.il — **בדיוק מי שהתיקון נועד לו** — הוא גם זה שהניווט שלו נוטה ליפול למטמון
// ולקבל את הקוד בלי הנפילה-לאחור. בלי הבאמפ התיקון לא היה מגיע לאיש, וזה היה נראה כמו
// "התיקון לא עבד" במקום "התיקון לא הגיע".
// §382ד — v65 (הערך נקרא מה-DOM לפני ה-await, והכתיבה לאוסף החדש קודמת לניקוי הישן).
// ⚠️ הבאג שזה מתקן **מחק נתון בפועל** משתי רשומות אמיתיות. ר' §382ד.
// §397ב — v79 (נפילה-לאחור לחיפוש לפי ownerMemberId — linkedBusinessId חסר לוותיקים).
// §397 — v78 (דשבורד העסק מקבל גם session של חבר, לצד הטוקן). ⚠️ **הבאמפ הוא חלק
// מהתיקון:** business-dashboard.html רושם SW בעצמו ונשלט על ידיו, ובעל-עסק שיקבל
// מהמטמון את ה-HTML הישן ימשיך לראות "קישור לא תקין" כשייכנס בלי טוקן — כלומר בדיוק
// המסלול החדש ייראה כאילו לא נבנה.
// §396 — v77 (מדידת מוכנות בעלי-העסק לכניסה כחבר, לקראת ביטול קישור-הטוקן §244).
// §395 — v76 (שורת-דליים גם ב"כל האוהדים" — §394 היה נגיש מדלת אחת בלבד).
// §394 — v75 (הפילוח הפך לניתן-לפעולה: שורה→רשימה, סינון-דלי ווואטסאפ על השורה).
// §393 — v74 (יומן מחיקות ושחזור: deletion-log.js חדש + שלוש הדלתות שמוחקות חבר/עסק).
// §390 — v73 (פילוח "אושרו ולא נכנסו" + פיל "מעולם לא נכנסו" ברשימת האוהדים).
// ⚠️ admin-dashboard.html נשלט ע"י ה-SW (scope "/") והניווט אליו network-first עם 4 שניות.
// §388ג — v72 (מסגרת הבאנר יושרה ל-.biz-card; הפס הצהוב ירד).
// §388ב — v71 (הסמל 🔗 הוסר מהבאנר, בקשת המשתמש — באותו קו של §379/§380).
// §388 — v70 (באנר חיבור-Google בדף הבית במקום פריט התפריט, ותיקון הקריאה שנכשלה
// בשקט). ⚠️ **הבאמפ הוא חלק מהתיקון:** ארבעה קבצים השתנו (שלושת הדפים + google-link.js),
// ו-google-link.js מוגש stale-while-revalidate — כלומר בלי החלפת שם-המטמון חבר שכבר
// ביקר היה מקבל מודול שאינו יודע להסתיר את הבאנר אחרי חיבור מוצלח.
// §387ב — v69 (נוסח "לא מחובר אצלנו" נכתב מחדש + קישור להרשמה). ⚠️ v68 כבר חי ולכן
// זה דורש באמפ משלו. הנוסח הישן שלח את מי שלא נרשם מעולם להשתמש בקוד שאין לו.
// §387 — v68 (כפתור "להמשיך עם Google" במודאל הכניסה + תיקון הנוסח ב-google-link.js).
// ⚠️ **הבאמפ כאן הוא חלק מהתיקון בשני מובנים:** (1) `google-link.js` מוגש
// stale-while-revalidate — כלומר חבר שכבר ביקר יקבל את **הנוסח הישן** ("המשך עם Google")
// שמצטט כפתור בשם שאינו קיים, וזה בדיוק הבאג שהשורה הזאת מתקנת. (2) `welcome.html` הוא
// דף-הכניסה: מי שהרשת שלו איטית נופל למטמון ומקבל מודאל בלי כפתור גוגל בכלל.
// §386 — v67 (גוגל בשני טופסי ההרשמה + google-signup.js חדש + קוביית "חשבון Google"
// במרכז הניהול). ⚠️ אותה מלכודת של §377: קובץ JS חדש נכנס ל-stale-while-revalidate ומגיע
// מהרשת בביקור הראשון — אבל **ה-HTML שמפנה אליו** הוא זה שנשלף מהמטמון, ובלי הבאמפ הכפתור
// לא היה מופיע לאיש. ובנוסף `admin-dashboard.html`: הוא נשלט ע"י ה-SW (scope "/") והניווט
// אליו network-first עם 4 שניות — כלומר בדיוק בבדיקה של §386 המנהל היה עלול לפתוח את
// המרכז הישן, לא לראות את הקובייה, ולהסיק שהקישור נכשל בזמן שהוא עבד.
// §385 — v66 (הנפילה-לאחור ל-raw.photoProofUrl הוסרה; bizProofs הוא המקור היחיד).
// כולל את §384 של הסשן המקביל — סנכרון קטלוג התגיות, שגם הוא ב-admin-dashboard.html.
// §382ג — v64 (שדה כתובת-הראיה הוצא מהבלוק שמוצג ללא-מנוי בלבד — אצל בעל-עסק שהוא
// מנוי הוא פשוט לא היה על המסך). ⚠️ v63 כבר חי, ולכן זה חייב באמפ משלו.
// §382 — נכנס ל-v63. ⚠️ `admin-dashboard.html` אינו רושם SW בעצמו אבל
// נשלט על ידיו (scope "/") והניווט אליו הוא network-first עם 4 שניות — מנהל על רשת איטית
// היה ממשיך לקרוא את ראיית-האימות מהשדה הישן ורואה "לא הועלתה" אחרי המיגרציה.
// §381 — v63 (התמונה מתחילה לעלות ברגע שנבחרה, ב-fan-register.html וב-business.html;
// ועיקר העניין: העלאה שהצליחה אחרי חריגת-זמן כבר לא נזרקת). ⚠️ **הבאמפ כאן קריטי
// במיוחד** — טופס ההרשמה הוא בדיוק הדף שנפתח פעם אחת ממכשיר לא-מוכר, ומי שכבר ביקר
// באתר יקבל את ה-HTML מהמטמון. בלעדיו התיקון פשוט לא יגיע לאף נרשם חדש.
// §380 — v62 (סבב שני של הסרת אימוג'ים ב-profile.html: כפתור "שמור תעודה לגלריה",
// "תקנון ומדיניות לאוהדים" ו"התנתקות"). §379 טיפל בכותרות הסקשנים בלבד, והמשתמש
// איתר את השלושה האלה אחריו. אותו נימוק לבאמפ: HTML במטמון ה-SW.
// §379 — v61 (הסרת האימוג'ים מארבע כותרות הסקשנים ב-profile.html: תעודת החבר שלי, פרטים
// אישיים, העסק שלי, הגדרות). שינוי חזותי בלבד — אבל הוא ב-HTML, ולכן הוא יושב במטמון
// ה-Service Worker של כל מי שכבר ביקר בדף. בלי הבאמפ הזה האימוג'ים נשארים על המסך אצל
// המשתמשים הקיימים, ויורדו רק אצל מי שנכנס לראשונה.
// §378 — v60 (סדר הכתובות: גוגל שנייה, workers.dev אחרונה). ⚠️ **הבאמפ כאן הוא חלק
// מהתיקון**: השינוי נוגע ב-**שבעה** קבצי HTML, וכולם נשלטים ע"י ה-SW. אוהד שמקבל מהמטמון
// את הסדר הישן ממשיך לחכות 11 שניות ל-workers.dev לפני שהוא מגיע לכתובת שעובדת — כלומר
// בדיוק ההמתנה שהתיקון בא לבטל, ואצל מי שהתיקון נועד לו.
// §374 — v58 (כתובת שלישית ל-API אצל גוגל, ב-welcome.html **וגם** ב-home.html, + מדידת-כתובת פר-ניסיון). ⚠️ **הבאמפ כאן הוא
// חלק מהתיקון ולא ניקיון, ובדיוק אצל מי שהתיקון נועד לו:** האוהד שהרשת שלו חוסמת את
// קלאודפלייר הוא גם זה שהניווט network-first שלו נוטה ליפול למטמון אחרי 4 שניות — כלומר
// בלי הבאמפ הוא היה ממשיך לקבל welcome.html עם **שתי** כתובות בלבד. בנוסף, תג-הסביבה
// מדווח 'sw-v58', וזה מה שיבדיל במדידה בין כשל של הכתובת השלישית לבין קוד ישן שלא מכיר אותה.
// §365 — v53 (סדר התצוגה ב"תקלות הרשמה": השורות לפני המשפכים). ⚠️ admin-dashboard.html
// אינו רושם SW בעצמו אבל נשלט על ידיו (scope "/", ר' §352), והניווט הוא network-first עם
// 4 שניות — כלומר מנהל על רשת איטית היה ממשיך לקבל בדיוק את המסך שעליו דיווח.
// §354 — v46 (כפתור-חזרה בטופס העסקים בכל מסלולי-הכניסה). ארבעה קבצי HTML השתנו:
// business.html, welcome.html, fan-register.html, terms.html. הבאמפ כאן הוא חובה ולא ניקיון —
// המשתמש שדיווח על המבוי-הסתום מריץ PWA מותקן, וניווט network-first שנופל למטמון היה מגיש
// לו את business.html **בלי** הכפתור, כלומר בדיוק את הבאג שדיווח עליו, אחרי הפריסה.
// §353 — v45 (רשת-הצלה לשלב ה-fetch בכניסה + משך-הזמן במדידה). ⚠️ **הבאמפ כאן הוא חלק מהתיקון
// ולא ניקיון:** תג-הסביבה של §338 מדווח את גרסת המטמון בפועל, ולכן 'sw-v45' הוא מה שיבדיל
// במדידה בין אוהד שכבר מריץ את רשת-ההצלה לאוהד שעדיין על v44 — בלעדיו אי אפשר יהיה לדעת אם
// כשל שנרשם אחרי הפריסה הוא כישלון של התיקון או פשוט קוד ישן. בדיוק הלקח של §352-המשך.
// §351+§352 — v44 (מרכז הנתונים: תקרת השאילתה, גבול-זמן ו"נסה שוב"). ⚠️ **admin-dashboard.html
// אינו רושם Service Worker בעצמו — אבל הוא כן נשלט על ידיו:** ההרשמה ב-home.html/welcome.html
// היא register('sw.js') בשורש, כלומר scope "/", והוא מיירט גם ניווטים לדפי-האדמין. הניווט
// כאן הוא network-first עם 4 שניות, ולכן מנהל על רשת איטית היה ממשיך לקבל את admin-dashboard
// הישן — **את הגרסה השבורה של §351 בדיוק** — כי המטמון הישן נמחק רק כשהשם הזה משתנה.
// §350 — v43 (רשת-הצלה לכניסה שמאחרת + פיצול ערוץ פסק-הזמן). §339 — v42 (הפצת תג הלוגו ל-3 המשטחים הנותרים). v41 = home.html בלבד. §338 — v40. פסק-זמן הניווט עלה מ-1.5 ל-4 שניות (ר' fetch למטה).
const CACHE_NAME = 'yz-shell-v91';
// §338 — פסק-הזמן של ניווטים. 1.5 שניות נבחרו כדי שדף לא "יתקע" על רשת גרועה, אבל בפועל
// זה נמדד קצר מדי: ניווט סלולרי רגיל חוצה אותו בקלות, וכל חצייה כזאת מגישה HTML **ישן**.
// אצל משתמש מנותק ב-PWA זה היה קטלני, כי דף הכניסה גם לא עדכן את ה-Service Worker (ר'
// §338 ב-welcome.html) — כלומר המטמון הישן גם לא נמחק אף פעם, וההגשה הישנה חזרה על עצמה
// בכל פתיחה. 4 שניות מכסות ניווט סלולרי אמיתי בלי לפגוע במה שפסק-הזמן נועד לו: offline
// אמיתי נכשל מיד ברמת הרשת ולא ממתין בכלל, ולכן הזמן הזה נגבה רק מחיבור גרוע-אך-קיים.
const NAV_TIMEOUT_MS = 4000;
// הגופנים נכנסים ל-precache: הם נדרשים לציור הראשון של דף-הנחיתה (§336, אירוח עצמי),
// ובלעדיהם הביקור הראשון-אחרי-התקנה עדיין משלם על הורדתם.
const PRECACHE_URLS = ['home.html', 'manifest.json', 'firebase-config.js',
                       'fonts/rubik-hebrew.woff2', 'fonts/rubik-latin.woff2'];

self.addEventListener('install', event => {
  event.waitUntil(
    caches.open(CACHE_NAME).then(cache =>
      Promise.all(PRECACHE_URLS.map(url => cache.add(url).catch(() => {})))
    )
  );
  self.skipWaiting();
});

self.addEventListener('activate', event => {
  event.waitUntil(
    caches.keys().then(keys =>
      Promise.all(keys.filter(k => k !== CACHE_NAME).map(k => caches.delete(k)))
    ).then(() => self.clients.claim())
  );
});

// רק same-origin GET — לא נוגעים בקריאות Firebase/Firestore/Brevo וכו' (אלה תמיד ישירות מהרשת, נתונים חיים).
//
// ניווט בין דפים (HTML, למשל home.html) — רשת קודם, עם timeout קצר (NAV_TIMEOUT_MS, ר. למעלה), גיבוי ל-cache:
// כך המשתמש כמעט תמיד מקבל את הגרסה העדכנית ביותר בפועל, בלי תלות ב-CACHE_NAME/עדכון
// Service Worker בכלל — לא ניתן יותר "להיתקע" על HTML ישן כי מישהו שכח להעלות מספר גרסה,
// ולא תלוי בכמה שקדנית מערכת ההפעלה בבדיקת עדכונים (ר' PROJECT_CONTEXT.md — אייפון עם
// PWA מותקן כמעט לא בודק עדכוני Service Worker מיוזמתו, אנדרואיד/כרום בודק כמעט בכל טעינה).
// רק אם הרשת לא עונה תוך NAV_TIMEOUT_MS (offline אמיתי/חיבור גרוע מאוד) — נופלים ל-cache כגיבוי.
//
// שאר הקבצים (JS/CSS/תמונות סטטיות) — stale-while-revalidate כרגיל: מגישים מיד מה-cache
// (מהירות מקסימלית, אלה כמעט אף פעם לא משתנים בין ביקורים), ומרעננים ברקע לפעם הבאה.
self.addEventListener('fetch', event => {
  const req = event.request;
  if (req.method !== 'GET' || new URL(req.url).origin !== self.location.origin) return;

  if (req.mode === 'navigate') {
    event.respondWith(
      caches.open(CACHE_NAME).then(async cache => {
        const cached = await cache.match(req);
        try {
          const controller = new AbortController();
          const timer = setTimeout(() => controller.abort(), NAV_TIMEOUT_MS);
          const res = await fetch(req, { signal: controller.signal });
          clearTimeout(timer);
          if (res.ok) cache.put(req, res.clone());
          return res;
        } catch (e) {
          // הבקשה-עם-timeout לא הספיקה תוך פסק-הזמן (בדיוק החיבור-האיטי שהיה אמור ליהנות הכי הרבה
          // מהתיקון האחרון) — באג-אמת שנתפס בפועל (2026-08-10): הניסיון-החוזר הזה מעולם לא נשמר
          // ב-cache, אז ביקור-איטי חוזר ממשיך לקבל תוכן ישן שוב ושוב, בלי שהמטמון מתעדכן.
          // עכשיו: הניסיון-החוזר (בלי timeout) תמיד ממשיך ברקע וכותב ל-cache כשהוא מסתיים —
          // בין אם הוגש עכשיו (אין cache בכלל) ובין אם הוגש cache-ישן והרשת עדיין ממשיכה ברקע.
          const retry = fetch(req).then(res => { if (res.ok) cache.put(req, res.clone()); return res; }).catch(() => null);
          if (cached) { retry.catch(() => {}); return cached; }
          return (await retry) || cached;
        }
      })
    );
    return;
  }

  event.respondWith(
    caches.open(CACHE_NAME).then(async cache => {
      const cached = await cache.match(req);
      const network = fetch(req)
        .then(res => { if (res.ok) cache.put(req, res.clone()); return res; })
        .catch(() => cached);
      return cached || network;
    })
  );
});

// ── Web Push — התראה שהוורקר שלח (worker/src/push.js), payload = {title, body, url} ──
// ⚠️ ידוע (2026-07-26): עברית מוצגת כ-"?" ב-iOS 26.5.2 — נבדק לעומק ונשלל שזו בעיה כאן
// (ר' ההערה ב-worker/src/push.js + docs/PROJECT_CONTEXT.md). לא לחזור לחקור בלי מידע חדש.
self.addEventListener('push', event => {
  const work = Promise.resolve(event.data ? event.data.text() : '')
    .then(raw => JSON.parse(raw))
    .catch(() => ({}))
    .then(data => self.registration.showNotification(data.title || 'Yellow Zone', {
      body: data.body || '',
      icon: 'icons/icon-192.png',
      badge: 'icons/icon-192.png',
      dir: 'rtl',
      lang: 'he',
      data: { url: data.url || 'home.html' },
    }));
  event.waitUntil(work);
});

self.addEventListener('notificationclick', event => {
  event.notification.close();
  const url = event.notification.data?.url || 'home.html';
  event.waitUntil(
    self.clients.matchAll({ type: 'window', includeUncontrolled: true }).then(clientsList => {
      const existing = clientsList.find(c => c.url.includes(new URL(url, self.location.href).pathname));
      if (existing) return existing.focus();
      return self.clients.openWindow(url);
    })
  );
});
