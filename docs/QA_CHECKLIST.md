# רשימת בדיקות ידניות — הבית הצהוב

> מסמך עבודה בלבד (לא חלק מתיעוד הארכיטקטורה — לזה יש את `PROJECT_CONTEXT.md`).
> מטרתו: לוודא שכל התרחישים המרכזיים באתר עובדים וזורמים מקצה לקצה, לפי סוג המשתמש.
> סמן ✔ ליד כל שורה שנבדקה ועובדת. שורה שנכשלה — תרשום ליד מה קרה בפועל, ונדבר עליה.

---

## 1. אורח (לא רשום בכלל)

- [x] הרשמת אוהד חדש דרך `fan-register.html` — מילוי מלא (פרטים אישיים, אימות אוהד בשני הנתיבים: מנוי / לא-מנוי, בני משפחה, מיקום, "בעל עסק?") ושליחה מצליחה *(✔ נבדק ע"י Claude, 2026-07-11 — לפני ואחרי הידוק ה-Rules)*
- [x] תמונת הוכחת אוהד (`fansPhoto`) מועלית בהצלחה ל-Storage כשלא מנוי *(✔ נבדק ע"י Claude, 2026-07-11)*
- [ ] יציאה מהטופס ל"תקנון" וחזרה — הטיוטה (כולל תמונה) נשמרת ולא נמחקת
- [ ] הרשמת עסק חדש דרך `business.html` — 4 השלבים + ולידציה חוסמת בכל שלב
- [ ] הרשמת עסק — חסימה אם אין תמונה מייצגת **וגם** לוגו (חובה לפחות אחת)
- [ ] הרשמת עסק של בן משפחה (שדה "קרבה לבעל המנוי")
- [x] ניסיון גישה ישירה ל-`home.html` בלי להתחבר → מפנה אוטומטית ל-`landing-demo.html` *(✔ נבדק ע"י Claude, 2026-07-11)*
- [x] ניסיון גישה ישירה ל-`admin.html`/`admin-members.html`/`admin-businesses.html` בלי להתחבר → מפנה ל-`landing-demo.html` *(✔ נבדק ע"י Claude, 2026-07-11)*
- [ ] `landing-demo.html` — גלילה חופשית, קרוסלת עסקים נבחרים, תצוגת אינדקס "חסומה" (לחיצה על כרטיס פותחת הודעת נעילה, לא את התוכן המלא)

## 2. אוהד מאושר (חבר)

- [x] התחברות מ-`landing-demo.html` (טלפון + קוד 6 ספרות) → מגיע ל-`home.html` *(✔ נבדק ע"י Claude, 2026-07-11)*
- [ ] "שכחתי קוד" ב-`landing-demo.html` — שולח בקשה, מוצגת הודעה (בלי לחשוף אם המספר קיים או לא)
- [ ] כניסה חוזרת ל-`landing-demo.html` אחרי שכבר מחובר → מצב "ברוכים השבים" (לא טופס הרשמה)
- [x] `home.html` — כל הסקציות נטענות (נבחרים / חדש / הטבות שוות / הכי אהובים / פופולריים / שאהבתי / אונליין / קרוב אליך / כל העסקים) *(✔ נבדק ע"י Claude, 2026-07-11)*
- [ ] חיפוש עסק לפי שם/קטגוריה עובד
- [ ] סינון לפי קטגוריה (עיגולי הקטגוריות) עובד
- [ ] לחיצה על עסק → bottom sheet נפתח עם כל הפרטים (שעות, כתובת, טלפון/וואטסאפ, גלריה)
- [ ] עסק פיזי/בעל מקצוע — מוצגת תעודת חבר מוטמעת ("למימוש ההטבה") בתוך הגיליון
- [ ] עסק אונליין עם קופון — כפתור "הצג קוד קופון" חושף את הקוד בלחיצה
- [x] סימון ⭐ לעסק (ולחיצה חוזרת מבטלת) — מתעדכן מיידית *(✔ נבדק ע"י Claude, 2026-07-11)*
- [ ] גלריית תמונות בעסק — lightbox עם ניווט בין תמונות
- [ ] הפעלת הרשאת מיקום מהתפריט → מגיע ל"קרוב אליך" עם עסקים לפי מרחק
- [ ] תפריט המבורגר — כל הפריטים עובדים (בית / פרטים אישיים / העסק שלי [רק לבעלי עסקים] / צור קשר / הוסף עסק / מיקום / אינסטגרם)
- [ ] "צור קשר" — הצגת גיליון עם מייל+אינסטגרם (לא מציג וואטסאפ לאוהד רגיל)
- [x] `profile.html` — פרטים אישיים נכונים, תעודת חבר מוטמעת נכונה (מספר חבר, שער אם מנוי) *(✔ נבדק ע"י Claude, 2026-07-11, כולל עריכת טלפון עצמית)*
- [ ] `profile.html` — הוספה/עריכה/מחיקה של בן משפחה
- [ ] `card.html` — תעודת חבר נטענת נכון + כפתור "שמור לגלריה" מייצר תמונה תקינה
- [ ] יציאה (`signOutUser`) מ-`profile.html` → מנקה session, מפנה ל-`landing-demo.html`, וניסיון חזרה ל-`home.html` אחרי היציאה מפנה שוב להתחברות

## 3. בעל עסק

- [x] כניסה לדשבורד עם קישור אישי (`business-dashboard.html?token=...`) *(✔ נבדק ע"י Claude, 2026-07-11, כולל כתיבת `pendingChanges`)*
- [ ] ניסיון גישה בלי טוקן / עם טוקן שגוי → מסך שגיאה (לא נכנס לדשבורד)
- [ ] עריכת כל אחד מ-6 הכרטיסים (בעל העסק / פרטי העסק / קטגוריה+שעות / הטבה / קישורים / מדיה) — כל שינוי מסתמן בכתום ובבדג' "שונה"
- [ ] "📤 סקירה ושליחה" — מסך הסיכום מציג נכון ישן→חדש לכל שדה ששונה
- [ ] שליחה בפועל → הודעת אישור בתוך אותו מסך, לא רק toast
- [ ] המשך עריכה כשיש כבר בקשה ממתינה לאישור מנהל — לא חסום, מתמזג לבקשה אחת
- [ ] העלאת לוגו/תמונה מייצגת/גלריה/סרטון בדשבורד — נשמר בפועל ל-Storage ומוצג בתצוגה המקדימה החיה
- [ ] קישור "פרופיל" מתוך הדשבורד → מציג את הפרופיל של בעל העסק הנכון (לא מישהו אחר)
- [ ] כפתור "העסק שלי" בתפריט הראשי (home/profile/card) מוביל לדשבורד הנכון של אותו בעל עסק

## 4. מנהל

- [x] כניסה כמנהל ל-`admin.html`/`admin-members.html`/`admin-businesses.html` — נטען תקין (עם `isAdmin:true`) *(✔ נבדק ע"י Claude, 2026-07-11)*
- [x] חבר עם session תקין אך בלי `isAdmin` → מנסה להיכנס לאדמין → מופנה בחזרה ל-`home.html` *(✔ נבדק ע"י Claude, 2026-07-11)*
- [ ] `admin-members.html` — אישור חבר בודד (כולל בני משפחה) → נוצר `loginCode` + נשלח מייל אוטומטי
- [ ] `admin-members.html` — דחיית חבר, מחיקת חבר
- [ ] `admin-members.html` — ביטול אישור (revoke) לבן משפחה ספציפי
- [ ] `admin-businesses.html` — אישור עסק בודד/bulk → מאשר אוטומטית גם את החבר המקושר (`ownerMemberId`)
- [ ] `admin-businesses.html` — דחיית עסק, עריכת כל שלב (inline), אישור/דחיית `pendingChanges`
- [ ] `admin-businesses.html` — טאב "ניהול סקשנים" — הפעלת/הסרת חדש/נבחר/הטבות שוות משפיעה בפועל על `home.html`
- [ ] `admin-businesses.html` — טאב "מרכז נתונים" — מספרי צפיות/כוכבים נכונים ומתעדכנים
- [ ] שליחת הודעת WhatsApp/מייל ידנית מתוך מודל האישור — הקישור לדשבורד (`{dashboard_link}`) וקוד הכניסה משובצים נכון
- [x] **(חדש)** ניהול מנהלים ב-`admin.html` — הענקת/ביטול `isAdmin` לחבר אחר, והרשימה מתעדכנת מיד *(✔ נבדק ע"י Claude, 2026-07-11)*

---

*עדכון אחרון: 2026-07-11*
