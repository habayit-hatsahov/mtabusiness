# Handoff: Yellow Zone — עיצוב מחדש (פיד הטבות)

## Overview
עיצוב מחדש של Yellow Zone — פלטפורמת ההטבות לאוהדי מכבי. החבילה מכסה את מסך הפיד (דסקטופ + מובייל), הבאנרים העליונים, כרטיס העסק וכרטיס פתוח (מודאל), הדר, פוטר ומצבי טעינה (skeleton).

## About the Design Files
הקבצים בחבילה הם **קובצי עיצוב (design references) שנכתבו ב-HTML** — פרוטוטייפים שמראים מראה והתנהגות מכוונים, **לא קוד פרודקשן להעתקה ישירה**. המשימה היא **לשחזר את העיצובים האלה בקוד הקיים של האתר** (React/Vue/וכו') לפי הדפוסים, הקומפוננטות וספריות ה-UI שכבר קיימים שם. אם אין עדיין סביבה — לבחור את הפריימוורק המתאים ולממש בו.

הערה טכנית: הקבצים הם `.dc.html` — HTML רגיל עם משתני CSS מוגדרים בבלוק `<style>` בראש הקובץ. אפשר לפתוח אותם בדפדפן. מאפיינים כמו `style-hover="..."` הם קיצור של הכלי שבו נבנה העיצוב — בקוד אמיתי הם פשוט `:hover`. ביטויים בסגנון `{{ ... }}` הם חיבורי state (לייק / גלילה) — ראו סעיף State.

## Fidelity
**High-fidelity.** צבעים, טיפוגרפיה, מרווחים, רדיוסים וצללים סופיים. יש לשחזר פיקסל-פרפקט, אך באמצעות הקומפוננטות והספריות הקיימות בקוד. תמונות העסקים הן **גרדיאנטים ממלאי־מקום** — יוחלפו בצילומים אמיתיים (`object-fit: cover`, יחס 4:3).

## Direction (חשוב לשמור)
- **דו-גוני בלבד: צהוב + כחול.** אין גוון שלישי בממשק. הצהוב `#FFD400` הוא צבע הפעולה (כפתורים, קווים מפרידים, אריח קטגוריה פעיל); הכחול `#0A2A66` הוא הדיו, הקווים והמסגור.
- RTL בכל המסמך (`dir="rtl"`).
- ההדר והפוטר לבנים, וכל אחד מהם ממוסגר ב**שני פסים 3px** — צהוב ואז כחול (בהדר מתחת לשורה, בפוטר מעליה).
- כותרות מקבלות "אנדרליין" קצר: 44×3px צהוב מתחת לכותרת הסקשן (במובייל 38×3px).

## Screens / Views

### 1. Header (דסקטופ ומובייל)
- **מטרה**: ניווט וחיפוש.
- **פריסה**: `display:flex; align-items:center; gap:18px; padding:7px 28px; background:#FFFFFF` (מובייל: `padding:0 16px 6px`, המבורגר מימין ולוגו משמאל, `justify-content:space-between`).
- **רכיבים**:
  - המבורגר: 40×40, אייקון 24px, `stroke-width:2`, צבע `#0A2A66`, hover: רקע `#F2F2EC`, radius 11px.
  - שדה חיפוש (דסקטופ): רוחב 300px, גובה 34px, רקע `#F2F2EC`, מסגרת `1px #E4E4DC`, radius 11px, טקסט 13.5px `#6B7A9B`, אייקון זכוכית 15px. במובייל: שורה מלאה מתחת להדר, גובה 34px, אותם צבעים.
  - לוגו: `uploads/YellowZone_horizontal_Logo.png` — PNG שקוף עם שוליים ריקים רחבים. בעיצוב הוא מוצג בגובה 96px (דסקטופ) / 72px (מובייל) עם `margin:-29px 0` / `-22px 0` כדי לחתוך את השוליים. **בקוד עדיף לייצא נכס חתוך** ולתת לו גובה אפקטיבי ~38px (דסקטופ) / ~30px (מובייל).
- **סגירת השורה**: `border-bottom:3px solid #FFD400` ומיד אחריו `div` בגובה 3px ברקע `#0A2A66`.

### 2. Categories row
- כותרת: "שלום רמי, מה מחפשים היום?" — 22px, `font-weight:700`, `letter-spacing:-0.01em`, `margin-bottom:16px`.
- שורה אופקית נגללת, **gap 6px** (הקטגוריות צמודות בכוונה), `overflow-x:auto`.
- כל פריט: עמודה ברוחב 78px (מובייל 70px), gap 8px/6px.
  - אריח: 56×56 (מובייל 46–52), radius 15px, אייקון 24px `stroke-width:1.9`.
  - האריח הראשון ("הכל") פעיל: רקע `#FFD400`, אייקון `#0A2A66`.
  - שאר האריחים לסירוגין: צהוב רך `#FFF3C9` / כחול רך `#E3E9F7`, אייקון `#7A5B00`.
  - האריח האחרון ("עוד"): רקע `#F2F2EC`, `opacity:.6`.
  - תווית: 12.5px (מובייל 11.5px), `font-weight:600`, `text-align:center`, `line-height:1.25`, צבע `#3A4C7A`.
- **אין קו מפריד מתחת לשורת הקטגוריות.**

### 3. Feed sections — "עסקים נבחרים", "חדש אצלנו"
- כותרת סקשן 20px/700 + אנדרליין 44×3px `#FFD400`. אין תת־כותרת.
- בצד השני: צ׳יפ "הכל ›" (גובה 32px, padding 0 13px, radius 999px, רקע `#FFF7DB`, מסגרת `1px #FFE9A3`, טקסט 13.5px/700 `#0A2A66`) + שני כפתורי חיצים עגולים 34×34 (רקע לבן, מסגרת `1px #E7E7E0`, hover רקע `#F2F2EC`).
- שורת כרטיסים: `display:flex; gap:18px; overflow-x:auto; padding-bottom:20px; scrollbar-width:none`.

### 4. Business card (הרכיב המרכזי)
- רוחב 238px (מובייל 216px, כרטיסי "חדש" קטנים 152px), רקע לבן, radius 16px, מסגרת `1px #ECEAE2`, `box-shadow:0 1px 2px rgba(10,42,102,.06)`, `overflow:hidden`.
- **תמונה**: יחס 4:3, edge-to-edge, ללא באדג׳. hover: התמונה `transform:scale(1.06)` ב-`.45s cubic-bezier(.2,.7,.3,1)`; הכרטיס `translateY(-3px)` + `box-shadow:0 12px 30px rgba(10,42,102,.14)` ב-`.22s ease`.
- **לב (מועדפים)**: עיגול 34×34 בפינה העליונה־שמאלית (10px), רקע `rgba(255,255,255,.68)` + `backdrop-filter:blur(10px)`, צל `0 2px 8px rgba(16,24,40,.18)`. האייקון 17px, `stroke:#0A2A66`; לא מסומן = `fill:none`, מסומן = `fill:#0A2A66`.
- **לוגו העסק**: ריבוע 34×34 (radius 9px) בפינה התחתונה־ימנית, רקע לבן, ראשי תיבות 13px/800 `#3A4C7A`.
- **טקסט** (padding 12px 16px 16px, gap 3px):
  - שם העסק 16px/700, `letter-spacing:-0.01em`, שורה אחת עם `text-overflow:ellipsis`.
  - קטגוריה 13px `#6B7A9B`.
  - הטבה ראשית 14px/700 `#0A2A66`, `margin-top:7px`, שורה אחת עם ellipsis (עד ~28 תווים).
  - "+ N הטבות נוספות" 12px/600 `#6B7A9B`.

### 5. Banners (ZoneHero.dc.html)
שלושה בלוקים בשורה, radius 20px:
- **עסק השבוע** — תמונה 42% מרוחב הבלוק + ציטוט בעל העסק ומקום מימוש.
- **הטבת השבוע** — על שדה צהוב רך `#FFF7DB` עם מסגרת `#FFE9A3`.
- **הצטרפו השבוע** — רשימת עסקים חדשים עם אווטאר 40×40 (radius 15px) ולינק "לכל החדשים".

### 6. Open business card (modal)
- Overlay `rgba(10,14,22,.55)`, הכרטיס ממורכז, radius 20px, `box-shadow:0 24px 60px rgba(10,14,22,.35)`.
- Hero תמונה בגובה 168–210px, כפתורי סגירה/לייק עגולים 34–36px באותו סגנון זכוכית.
- גוף: שם העסק 26px/700, צ׳יפ קטגוריה (רקע `#F2F2EC`, radius 999px), כתובת ושעות 13.5px `#6B7A9B`.
- "ההטבות שלך": ההטבה הראשית בקופסה `#FFF7DB` עם מסגרת `#FFE9A3`; הטבות נוספות בקופסה לבנה עם מסגרת `#E7E7E0`. radius 16px, padding 14px.
- CTA: "הצג תעודת אוהד" — גובה 50–52px, radius 13px, רקע `#FFD400`, טקסט `#0A2A66` 16px/700, אייקון 18px. מתחתיו שני כפתורי משנה ("התקשר", "ניווט") בגובה 46px, מסגרת `1px #E7E7E0`.

### 7. Skeleton loading
אותה גיאומטריה בדיוק כמו הכרטיס: תמונה 4:3 + שלוש שורות טקסט. שימר: `linear-gradient(90deg,#EFEFE9 25%,#FAFAF6 37%,#EFEFE9 63%)`, `background-size:200% 100%`, אנימציה `1.4s linear infinite` מ-`background-position:200% 0` ל-`-200% 0`, עם השהיות מדורגות (0 / .15s / .3s).

### 8. Footer (שורה דקה בלבד)
- מעל השורה: פס 3px `#FFD400` ואז פס 3px `#0A2A66`.
- השורה עצמה: רקע לבן, `padding:10px 28px` (מובייל `8px 16px`), `display:flex; align-items:center; justify-content:space-between`.
- תוכן: לוגו בצד אחד; בצד השני **רק** "אינסטגרם" (עם אייקון 17px) ו-"תנאי שימוש" — 12.5px/600 `#0A2A66`, hover `opacity:.6`. כל שאר הניווט חי בתפריט ההמבורגר ולא משוכפל כאן.

## Interactions & Behavior
- **לייק**: לחיצה על העיגול מחליפה `fill` בין `none` ל-`#0A2A66`. אין ניווט (stopPropagation מהכרטיס).
- **כרטיס**: לחיצה פותחת את המודאל של העסק.
- **חיצי שורה**: גוללים את שורת הכרטיסים ב-~260px (`scrollBy`, `behavior:'smooth'`).
- **גלילה**: שורות הכרטיסים והקטגוריות נגללות אופקית; פס הגלילה מוסתר.
- **Hover**: כרטיס — הרמה + הגדלת תמונה (ערכים למעלה); כפתורים ואריחים — רקע `#F2F2EC`.
- **Focus**: יש להוסיף `:focus-visible { outline:2px solid #0A2A66; outline-offset:2px }` לכל אלמנט אינטראקטיבי (לא מומחש בפרוטוטייפ).
- **רספונסיביות**: דסקטופ ~2 כרטיסים גלויים ברוחב הקונטיינר (1180px), מובייל 1.5 כרטיסים (כרטיס 216px ברוחב מסך 390px).

## State Management
- `favorites: Record<businessId, boolean>` — מצב הלב לכל כרטיס.
- `openBusinessId: string | null` — איזה עסק פתוח במודאל.
- `activeCategory: string` — הקטגוריה הנבחרת (ברירת מחדל "הכל").
- `searchQuery: string`.
- `isLoading` לכל סקשן — מציג skeletons באותה גיאומטריה.
- שליפת נתונים: רשימת עסקים לפי קטגוריה/חיפוש, "חדש אצלנו", ובאנרים (עסק השבוע / הטבת השבוע / הצטרפו השבוע).

## Design Tokens
```
--bg          #F6F6F2   רקע כללי
--surface     #FFFFFF   כרטיסים, הדר, פוטר
--ink         #0A2A66   טקסט ראשי / כחול המותג
--ink-2       #3A4C7A   טקסט משני
--ink-3       #6B7A9B   טקסט שלישוני
--line        #E7E7E0   קווים ומסגרות
--field       #F2F2EC   שדות ו-hover
--field-line  #E4E4DC   מסגרת שדה
--acc         #FFD400   צהוב פעולה
--acc-ink     #0A2A66   טקסט על צהוב
--acc-text    #0A2A66   טקסט הדגשה (הטבה)
--acc-soft    #FFF7DB   שדה צהוב רך
--acc-line    #FFE9A3   מסגרת על שדה צהוב
--like        #0A2A66   לב מועדפים
--cat-ink     #7A5B00   אייקון קטגוריה
--cat tiles   #FFF3C9 / #E3E9F7 (לסירוגין)
--skeleton    #EFEFE9 → #FAFAF6

Radius:  card 16 · tile 15 · field 11 · button 13 · shell 20 · modal 20 · chip 999
Spacing: 6 (קטגוריות) · 12 · 14 · 16 · 18 · 22 · 28
Shadow:  sm 0 1px 2px rgba(10,42,102,.06) · hover 0 12px 30px rgba(10,42,102,.14) · modal 0 24px 60px rgba(10,14,22,.35)
Type:    Rubik (400/500/600/700/800). כותרות 700, letter-spacing -0.01em.
         22 כותרת מסך · 20 כותרת סקשן · 16 שם עסק · 14 הטבה · 13/13.5 משני · 12/12.5 תווית
```

## Assets
- `uploads/YellowZone_horizontal_Logo.png` — לוגו YellowZone אופקי (PNG שקוף, 1920×1080, עם שוליים ריקים רחבים; מומלץ לייצא גרסה חתוכה, ואם אפשר גם גרסה מונוכרום כחולה לשימוש על רקע צהוב).
- אייקונים: Lucide (המבורגר, חיפוש, לב, חצים, קטגוריות, אינסטגרם) — בפרוטוטייפ הם SVG inline; בקוד להשתמש בספריית האייקונים הקיימת.
- תמונות עסקים: ממלאי מקום (גרדיאנטים) — יוחלפו בצילומים.
- פונט: Rubik מ-Google Fonts.

## Files
- `ZoneFeedGold.dc.html` — **המסך הראשי**: הדר, קטגוריות, שתי שורות כרטיסים, skeletons, פוטר, וגרסת מובייל (כולל מודאל).
- `ZonePhonePrev.dc.html` — מסך מובייל וכרטיס עסק פתוח בגדול.
- `ZoneHero.dc.html` — שלושת הבאנרים העליונים.
- `Yellow Zone Palettes.dc.html` — השוואת שלוש פלטות צהוב־כחול (נבחרה פלטה א׳ — מכבי). רפרנס בלבד.
- `uploads/YellowZone_horizontal_Logo.png` — הלוגו.

## Screenshots
- `screenshots/01-feed-desktop-and-mobile.png` — הפיד המלא: הדר, קטגוריות, שורות כרטיסים, skeletons, פוטר, וגרסת המובייל.
- `screenshots/02-mobile-and-open-card.png` — מסך מובייל וכרטיס עסק פתוח.
- `screenshots/03-banners.png` — שלושת הבאנרים העליונים.
